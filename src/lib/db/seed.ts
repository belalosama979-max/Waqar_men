export const dbSeed = {
  async init() {
    // In Supabase, the database is seeded using the SQL Editor.
    // This function is kept for compatibility with the existing architecture.
    return Promise.resolve();
  }
};
